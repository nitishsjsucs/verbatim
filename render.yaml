services:
  - type: web
    name: govinda-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app_backend.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
